#include "poids.h"
#include <arete.h>
Poids::Poids()
{

}
void Poids::mouseDoubleClickEvent(QGraphicsSceneMouseEvent *event){
//    bool ok = false;
//    int newPoid = QInputDialog::getInt(nullptr, "poids", "Quel est le poids ?", arete::poid, 0,2000,1, &ok);

//    QGraphicsTextItem::mouseDoubleClickEvent(event);

//    arete::setPoid(10);
//    update();
}
